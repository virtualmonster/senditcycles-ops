version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: senditcycles-db-staging
    environment:
      POSTGRES_USER: ${DB_USER:-senditcycles}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-senditcycles_staging}
    volumes:
      - postgres_data_staging:/var/lib/postgresql/data
      - ../../../senditcycles/server/database/init.sql:/docker-entrypoint-initdb.d/01-init.sql
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-senditcycles}"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - senditcycles-staging

  backend:
    build:
      context: ../../../senditcycles/server
      dockerfile: Dockerfile
    container_name: senditcycles-api-staging
    environment:
      NODE_ENV: staging
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-senditcycles}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-senditcycles_staging}
      JWT_SECRET: ${JWT_SECRET}
      LOG_LEVEL: info
    expose:
      - "3001"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - senditcycles-staging

  frontend:
    build:
      context: ../../../senditcycles/client
      dockerfile: Dockerfile
    container_name: senditcycles-web-staging
    environment:
      VITE_API_URL: http://staging-api.senditcycles.internal/api
      NODE_ENV: staging
    expose:
      - "3000"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - senditcycles-staging

  # Load balancer (Nginx reverse proxy)
  # Demonstrates scaling: multiple backend instances can be added to upstream
  load-balancer:
    image: nginx:alpine
    container_name: senditcycles-lb-staging
    ports:
      - "3010:80"
    volumes:
      - ./config/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend
    restart: unless-stopped
    networks:
      - senditcycles-staging
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3

volumes:
  postgres_data_staging:
    driver: local

networks:
  senditcycles-staging:
    driver: bridge
